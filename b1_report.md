구글 시트를 사용하지 않고 웹사이트에서 직접 이메일 데이터만 받아와 처리하는 구조로 내용(프로젝트 1)을 수정했습니다. 요구사항에 맞춰 정제된 최종본입니다.

---

## [프로젝트 1] 자동화 도구 비교 구현 (Make vs n8n)

**워크플로우:** "웹사이트 뉴스레터 구독 폼 제출(Webhook) → 이메일 값 존재 여부 분기 → (존재 시) 매월 1일 뉴스레터 발송 / (누락 시) 관리자 알림 메일 발송"

### 1. Make 구현 로직

- **Trigger:** Webhooks – Custom Webhook (웹사이트 구독 폼과 연동하여 사용자 입력 데이터 수신)
- **Router:** 수신된 데이터 중 '이메일(Email)' 필드의 값이 '존재함(Exists)' / '존재하지 않음(Does not exist)'인지 조건 분기
- **Action 1 (존재 시 경로):** Email – Send an Email (수신한 이메일 주소로 타겟팅하여 매월 1일 뉴스레터가 발송되도록 딜레이/스케줄링 처리)
- **Action 2 (누락 시 경로):** Gmail – Send an Email (데이터 수집 오류 방지를 위해 지정된 관리자 이메일로 누락 경고 메일 발송)
- _[Make 워크플로우 구성 화면 및 각 분기별 실행 결과 캡처 첨부]_

### 2. n8n 구현 로직

- **Trigger:** Webhook Node (웹사이트 내부 폼에서 전송되는 이벤트 및 데이터 수신)
- **Filter (분기):** IF Node – 조건식을 통해 이메일 필드값이 `empty`인지 판별 (True/False)
- **Action 1 (False/이메일 존재):** Send Email Node (Schedule/Cron 로직과 연계하여 수집된 구독자 이메일로 매월 1일 발송되도록 구성)
- **Action 2 (True/이메일 누락):** Send Email Node (관리자 이메일 주소로 고정하여 경고 메시지 발송)
- _[n8n 워크플로우 구성 화면 및 각 분기별 실행 결과 캡처 첨부]_

### 3. 비교 분석 보고서

| 비교 항목            | Make                                                        | n8n                                                          |
| -------------------- | ----------------------------------------------------------- | ------------------------------------------------------------ |
| **UI/UX**            | 시각적이고 애니메이션이 풍부한 둥근 노드 기반               | 직관적이고 깔끔한 흐름도(Flowchart) 스타일                   |
| **설정 난이도**      | 비개발자도 직관적으로 이해하기 쉬운 낮은 진입장벽           | 변수 맵핑 시 JSON 구조 이해 등 약간의 개발 지식 필요         |
| **연동 서비스 범위** | 1,000개 이상의 방대한 사전 구축 앱 생태계 보유              | 사전 구축 앱은 적은 편이나 HTTP Request로 유연하게 보완 가능 |
| **무료 플랜 범위**   | 클라우드 기반 월 1,000 Ops / 시나리오 2개로 제한적          | 자가 호스팅(Self-hosting) 구축 시 기능 및 실행 횟수 무제한   |
| **실행 로그 확인**   | History 탭에서 각 모듈별 입출력 데이터를 돋보기 형태로 확인 | Executions 탭에서 노드별 JSON 데이터를 트리 구조로 상세 확인 |

**장단점 및 적합한 상황 판단**

- **Make:** 설정이 매우 직관적이고 마우스 조작만으로 데이터 맵핑이 쉬워, 코딩 지식이 없는 마케터나 기획자가 빠르게 자동화 파이프라인(MVP)을 구축할 때 가장 적합합니다.
- **n8n:** 서버에 직접 설치해 무료로 무제한 사용할 수 있고 복잡한 커스텀 로직을 유연하게 짤 수 있어, 데이터 보안이 중요하거나 장기적인 운영비 절감이 필요한 개발/IT 중심 팀에 적합합니다.
