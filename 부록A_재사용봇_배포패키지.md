# 부록 A — 재사용 봇 배포 패키지 (보너스 과제 1)

> **보너스 1 요구사항**: 커스텀 인스트럭션 또는 GPTs/에이전트 기능으로 최종 프롬프트를 "재사용 가능한 형태"로 배포한다.

| 항목 | 내용 |
|---|---|
| 배포 대상 | [02_시스템설계문서.md 7장](02_시스템설계문서.md#7-최종-시스템-프롬프트-전문-v2)의 v2 최종 프롬프트 |
| 작성일 | 2026-07-27 |
| 배포 형태 | 3개 플랫폼용 인스트럭션 + 사용자 배포 안내문 |

> **⚠️ 플랫폼 사양에 대한 주의**
> 아래 각 플랫폼의 입력란 이름·글자수 제한·메뉴 위치는 **수시로 바뀐다.** 이 문서의 수치는 참고용이며, 실제 배포 시 각 플랫폼 화면에서 직접 확인해야 한다. 이 문서 자체가 "확인되지 않은 수치는 단정하지 않는다"는 [메일라이트 규칙](02_시스템설계문서.md#4-시스템-프롬프트-설계)을 따른다.

---

## 1. 왜 "재사용 가능한 형태"가 필요한가

프롬프트를 문서로만 남기면 실제로는 아무도 안 쓴다. 매번 1,900자를 복사해 붙여넣어야 하고, 누군가는 중간을 빠뜨린 채 쓰기 때문이다. **한 번 등록해 두고 대화만 시작하면 되는 형태**로 만들어야 팀 표준이 된다.

배포 시 반드시 함께 나가야 하는 것은 프롬프트가 아니라 **입력 템플릿**이다. 타겟 사용자(주니어 담당자)는 프롬프트를 고칠 줄 모른다고 가정했으므로, 그가 할 일은 템플릿을 채우는 것 하나여야 한다.

---

## 2. 배포판 A — ChatGPT / GPTs

### 2.1 등록 위치

| 입력란 | 넣을 내용 |
|---|---|
| Name | `메일라이트 (MailWright)` |
| Description | 아래 2.2 |
| Instructions | 아래 2.3 (v2 프롬프트 + 배포 전용 서두) |
| Conversation starters | 아래 2.4 |
| Capabilities | **Web Browsing / Code Interpreter 모두 끄기** — 이 봇은 사용자가 준 정보만 다뤄야 한다. 검색이 켜져 있으면 "확인 필요"로 내려야 할 항목을 검색으로 채워 버릴 수 있다 |
| Knowledge (파일 업로드) | 사내 메일 가이드·금지어 목록이 있다면 업로드. **없으면 비워 둔다** (없는 파일을 있는 척하면 봇이 그것을 근거로 삼는다) |

### 2.2 Description

```
확인되지 않은 수치와 정책이 메일에 들어가지 않게 막아 주는 B2B 고객 메일 초안 도우미입니다.
9개 항목을 채우면 제목 3안·본문·다음 액션·확인 필요 항목을 정해진 형식으로 만들어 줍니다.
정보가 부족하면 지어내지 않고 되묻습니다.
```

### 2.3 Instructions

> [02_시스템설계문서.md 7장](02_시스템설계문서.md#7-최종-시스템-프롬프트-전문-v2)의 v2 전문을 그대로 넣되, **마지막 줄만 교체**한다. 배포판에서는 사용자가 시스템 프롬프트를 붙여넣지 않으므로 "준비 완료라고만 답하고 대기하세요"가 불필요하다.

**교체할 마지막 줄**

| 원본 (v2) | 배포판 |
|---|---|
| `준비되었으면 "준비 완료"라고만 답하고 대기하세요.` | 아래 블록으로 교체 |

```
# 대화 시작 시 동작
사용자가 처음 말을 걸면, 설명 없이 아래 입력 템플릿을 먼저 제시하고 채워 달라고 요청한다.
사용자가 템플릿 없이 자유롭게 요청하면, 안전장치 규칙에 따라 확인 질문 3개로 시작한다.

[수신자]
[관계·직급]
[메일 목적]
[확인된 사실]
[핵심 포인트 3개]
[톤]
[금지어]
[필수 포함 항목]
[마감·회신기한]

모르는 항목은 '미정'이라고 적어도 됩니다 — 이 안내를 템플릿과 함께 보여준다.
```

### 2.4 Conversation starters (4개)

```
장애 사과 메일을 써야 합니다
요금·정책 변경 안내 메일이 필요합니다
일정 협의 요청 메일 초안을 부탁합니다
입력 템플릿을 보여주세요
```

### 2.5 공유 설정

| 옵션 | 권장 | 이유 |
|---|---|---|
| 나만 보기 | 초기 검증 단계 | 팀 배포 전 본인이 5~10건 먼저 써 본다 |
| **링크가 있는 사용자** | **팀 배포 시 권장** | 사내 위키에 링크만 걸면 된다 |
| 공개 (GPT Store) | 비권장 | 사내 업무 맥락이 담긴 봇을 공개할 이유가 없다 |

---

## 3. 배포판 B — Claude / Projects

Claude에는 GPTs와 대응되는 **Projects**가 있다. 프로젝트 단위로 커스텀 인스트럭션과 참고 자료를 묶어 두고, 그 안에서 대화를 시작하는 방식이다.

### 3.1 등록 위치

| 항목 | 넣을 내용 |
|---|---|
| 프로젝트 이름 | `메일라이트 — 고객 메일 초안` |
| 프로젝트 설명 | 2.2의 Description 재사용 |
| **커스텀 인스트럭션** | v2 프롬프트 전문 + 2.3의 "대화 시작 시 동작" 블록 |
| 프로젝트 지식 | 사내 메일 가이드·금지어 목록·과거 우수 사례 (있는 경우에만) |

### 3.2 Claude Projects에서 특히 유리한 점

프로젝트 지식에 **실제 사내 문서를 올려 두면** 이 봇의 가장 큰 약점이 줄어든다. 예컨대 환각 검증 Q2(SLA 예외 조항)에서 봇이 "답할 수 없다"고 한 이유는 계약서가 없었기 때문이다. SLA 표준 템플릿을 프로젝트 지식에 넣어 두면, 같은 질문에 근거를 갖고 답할 수 있게 된다.

**단, 이때 프롬프트에 한 줄을 추가해야 한다.**

```
# 프로젝트 지식 사용 규칙
- 프로젝트 지식에 있는 내용은 근거로 인용할 수 있다. 인용 시 어느 문서의 어느 항목인지 밝힌다.
- 프로젝트 지식에 없는 내용은 여전히 '확인 필요'로 처리한다.
- 프로젝트 지식과 사용자 입력이 충돌하면, 사용자에게 어느 쪽이 최신인지 확인 질문을 한다.
```

이 규칙이 없으면 봇이 지식 문서의 내용과 자기 추측을 구분하지 않게 된다.

---

## 4. 배포판 C — Gemini / Gem

Gemini에는 **Gem**이라는 커스텀 어시스턴트 기능이 있다.

| 항목 | 넣을 내용 |
|---|---|
| Gem 이름 | `메일라이트` |
| 지침(Instructions) | v2 프롬프트 전문 + 2.3의 "대화 시작 시 동작" 블록 |
| 참고 파일 | 사내 가이드가 있는 경우에만 |

> **글자수 제한 주의**: Gem 지침란의 최대 길이는 플랫폼 정책에 따라 다르고 변경될 수 있다. v2 프롬프트는 약 1,900자이므로 대체로 문제가 없으나, **잘린 채 저장되면 안전장치 섹션이 통째로 사라질 수 있다.** 저장 후 반드시 `# 우선순위` 줄까지 남아 있는지 눈으로 확인한다.

---

## 5. 최소 배포판 — 커스텀 인스트럭션만 쓰는 경우

GPTs·Projects·Gem을 쓸 수 없는 환경(무료 요금제 등)에서는 **커스텀 인스트럭션 칸**에 축약본을 넣는다. 이 칸은 길이 제한이 빡빡하므로 v2를 그대로 넣기 어렵다.

### 5.1 축약 우선순위 — 무엇을 먼저 버리는가

| 순위 | 섹션 | 판단 |
|---|---|---|
| **끝까지 남긴다** | 사실·수치·정책 처리 규칙 | 이게 없으면 봇의 존재 이유가 사라진다 |
| **끝까지 남긴다** | 안전장치 (확인 질문·미정 처리) | |
| 남긴다 | 출력 형식 5블록 (제목만) | 세부 글자수 규칙은 버려도 됨 |
| 먼저 버린다 | 작업 절차 5단계 | 형식 규칙이 살아 있으면 어느 정도 대체됨 |
| 먼저 버린다 | 역할 설명의 수식어 | |

### 5.2 축약본 (약 700자)

```
당신은 B2B 고객 메일 초안을 쓰는 CS 리드 "메일라이트"입니다.

출력은 항상 아래 5블록 순서로만 합니다.
1. 제목 3안 / 2. 본문 / 3. 다음 액션(체크박스, 최대 4개) /
4. 확인 필요 / 5. 이번 턴 반영 근거(3개, 각 30자 이내)

[사실 처리 — 가장 중요]
금액·비율·날짜·소요시간·고객수·SLA·법령·사내규정은 사용자가 명시적으로 준 것만 본문에 씁니다.
안 준 수치는 [출처 확인 필요: 항목명]으로 표기하고 절대 숫자를 채우지 않습니다.
사용자가 근거 없는 전제를 깔면(예: "지난번 그 수치") 쓰지 말고 출처를 되묻습니다.
"4. 확인 필요"에는 `항목 / 어디서 확인 / 발송 시 위험` 3단으로 적고, 없으면 "없음"이라고 씁니다.

[안전장치]
메일 성패를 좌우하는 정보가 없으면 초안 전에 확인 질문을 최대 3개까지 합니다.
각 질문 옆에 "(모르면 '미정'이라고 답해 주세요)"를 붙입니다.
"미정"이면 추측하지 말고 본문에 [○○ 확인 후 기입] 자리표시자를 넣습니다.

[금지] 추측성 단정, 이모지, 과장 표현, 사과 3회 초과, 실명·연락처 생성, 사고 과정 노출
[우선순위] 사실 정확성 > 형식 준수 > 친절함 > 분량
```

---

## 6. 사용자 배포 안내문 (그대로 사내 공유용)

> 아래는 봇 링크와 함께 팀에 전달할 안내문이다. 프롬프트 설명이 아니라 **사용법만** 담았다.

```
[안내] 고객 메일 초안 도우미 "메일라이트" 사용 안내

■ 무엇을 하는 봇인가
고객사에 나가는 사과·안내·요청 메일 초안을 만들어 줍니다.
가장 중요한 기능은 "확인 안 된 수치와 정책을 본문에 넣지 않는 것"입니다.

■ 사용법 — 아래 템플릿을 채워서 붙여넣으면 끝입니다
[수신자]
[관계·직급]
[메일 목적]
[확인된 사실]
[핵심 포인트 3개]
[톤]
[금지어]
[필수 포함 항목]
[마감·회신기한]

■ 작성 규칙 3가지
1. [확인된 사실]에는 출처를 댈 수 있는 것만 적습니다. 애매하면 비워 두세요.
2. 모르는 항목은 지우지 말고 "미정"이라고 적으세요. 지우면 봇이 항목 자체를 잊습니다.
3. 숫자는 단위까지 적으세요. ("2시간" → "14:10~16:35, 총 2시간 25분")

■ 결과를 받으면 반드시 할 일
"4. 확인 필요" 블록을 먼저 읽으세요. 여기 있는 항목은 아직 근거가 없는 것들입니다.
확인 전에 발송하면 정정 메일을 다시 보내야 할 수 있습니다.

■ 봇이 되물으면
정보가 부족하다는 뜻입니다. 답할 수 있는 것만 답해도 초안을 만들어 줍니다.

■ 주의
- 이 봇은 메일을 발송하지 않습니다. 초안만 만듭니다.
- 사내 규정·계약서 내용은 알지 못합니다. 필요하면 직접 확인해야 합니다.
- 고객 실명·연락처는 입력하지 마세요. "담당자", "COO"처럼 역할명으로 적습니다.
```

---

## 7. 배포 후 관리

| 주기 | 할 일 |
|---|---|
| 배포 직후 | 본인이 실제 업무 메일 5~10건을 이 봇으로 작성해 본다. 실패 사례를 모은다 |
| 실패 사례 3건 누적 시 | [kit/03_환각검증_질문지.md](kit/03_환각검증_질문지.md)에 해당 유형 문항을 추가하고, 프롬프트를 v3로 개정 |
| 모델 버전 변경 시 | **환각 6문항을 다시 돌린다.** 모델이 바뀌면 이전 검증 결과는 무효다 |
| 분기 1회 | Few-shot 예시를 실제 우수 사례로 교체. 현재 3개는 사과·안내·요금 상황에 치우쳐 있다 |

### 이미 예정된 개정 사항 (v3)

[03_실행로그_정리본.md](03_실행로그_정리본.md#v3-개선-항목-다음-반복에서-적용)에서 도출된 3개 규칙을 다음 배포 시 반영한다.

1. `[부록]` 분리 규정 — 5블록에 담기지 않는 요청 처리
2. 정보 제외 시 명시 규정 — 사용자가 준 정보를 뺄 때 이유를 밝히도록
3. 부분 출력 규정 — 단일 항목 문의에는 해당 블록만
